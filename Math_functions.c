#include<stdio.h>
#include<math.h>
int main(){
    // int x = 4;
    float y = 3;

    // x = sqrt(x);
    // x = pow(x,2);
    // y = round(y);
    // y = ceil(y);
    // y = floor(y);
    // x = abs(x);
    // y = log(y);
    // y = sin(y);
    // y = cos(y);
    // y = tan(y);





    // printf("%d", x);
    printf("%f",y);

    return 0;
}